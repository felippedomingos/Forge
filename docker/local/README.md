# Moved

This directory's `docker-compose.yml` (Postgres/Temporal/Temporal UI only) has been
folded into the single `docker-compose.yml` at the repository root, which also adds
`forge-api`, `forge-worker`, and `frontend` - see the root `README.md` and
`docs/015-Deployment.md` for the current setup.
